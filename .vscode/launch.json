{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "attach",
            "port" : 9222,
            "url": "",
            "name": "Attach to Chrome",
            "webRoot": "${workspaceRoot}"
        }
    ]
}